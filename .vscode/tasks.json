{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nim: run tests",
            "command": "nimble",
            "args": ["test", "-o:bin/"],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "type": "shell",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}